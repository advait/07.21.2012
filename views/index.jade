// index.jade
// Copyright 2012 Compucius

extends layout

block scripts
  != js('webworker')

block content
  if context.user
    p
      h2 Welcome #{context.user.name}!
      ul
        li user/fb id: #{context.user.id}
